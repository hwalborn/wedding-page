import React from 'react'
import { Row } from 'react-bootstrap'

const ListContainer = ({positionClass}) => {
      return (
        <div className={positionClass} id="location-container">
            <h1>HI</h1>
        </div>
      )
  }
  
  export default ListContainer